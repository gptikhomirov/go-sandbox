/*
# 08. Деактивировать пользователей с заблокированными счетами

Для всех пользователей, у которых есть **заблокированный** счёт (`accounts.is_blocked = true`), проставить `is_active = false`. Вернуть `id` и `name`.

**Таблицы:** `users`, `accounts`
*/

-- TODO
