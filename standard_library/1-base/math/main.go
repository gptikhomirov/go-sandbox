/*
Пакет math — математические функции для ДРОБНЫХ чисел (тип float64).

Важно понять сразу: math работает с дробными числами (float64), а НЕ с целыми (int).
Для целых чисел в Go есть встроенные min() и max() — пакет math для них не нужен.

Зачем нужно: корень, степень, округление, тригонометрия и т.п.

Как запустить:  go run main.go
*/
package main

import (
	"fmt"
	"math"
)

func main() {
	// Abs — модуль (убирает минус). Принимает и возвращает дробное число.
	fmt.Println(math.Abs(-3.5)) // => 3.5

	// Округления:
	//   Ceil  — вверх   (2.1 -> 3)
	//   Floor — вниз    (2.9 -> 2)
	//   Round — к ближайшему целому (2.5 -> 3)
	fmt.Println(math.Ceil(2.1), math.Floor(2.9), math.Round(2.5)) // => 3 2 3

	// Sqrt — квадратный корень. Pow — возведение в степень (x в степени y).
	fmt.Println(math.Sqrt(16))  // => 4
	fmt.Println(math.Pow(2, 10)) // => 1024  (2 в степени 10)

	// Для ЦЕЛЫХ чисел используем встроенные min/max — без math:
	fmt.Println(min(3, 9)) // => 3
	fmt.Println(max(3, 9)) // => 9

	// Полезная константа: math.MaxInt — самое большое целое число.
	// Частый приём: начать поиск минимума с «очень большого» значения и уменьшать его.
	smallest := math.MaxInt
	for _, v := range []int{7, 3, 9, 1} { // range перебирает элементы списка
		smallest = min(smallest, v)
	}
	fmt.Println(smallest) // => 1
}

/*
Что важно запомнить:
  • math — для ДРОБНЫХ чисел (float64): Sqrt, Pow, Ceil/Floor/Round.
  • Для ЦЕЛЫХ (int) бери встроенные min()/max(), а не math.Max/math.Min.
  • math.MaxInt — «самое большое целое»; удобно как стартовое значение при поиске минимума.
  • Дробные числа неточны: 0.1 + 0.2 не равно ровно 0.3. Для денег считай в копейках (целыми).

Маленькие задачи:
  1) Посчитай длину гипотенузы для катетов 3 и 4: math.Sqrt(3*3 + 4*4). Должно получиться 5.
  2) Найди максимум среди чисел 4, 8, 2 с помощью встроенного max.
*/
