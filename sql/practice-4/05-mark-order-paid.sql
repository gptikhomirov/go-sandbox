/*
# 05. Перевести заказ в paid (state transition)

Перевести заказ `id = 2` из `pending` в `paid`. **Если он уже не `pending`** — ничего не менять. Проставить `paid_at = NOW()`. Вернуть `id`, `status`, `paid_at`.

**Таблица:** `orders`
*/

-- TODO
